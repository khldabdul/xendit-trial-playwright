/**
 * Playwright Configuration (Minimal Stub)
 *
 * NOTE: This project uses Cucumber BDD for test execution.
 * Test configuration is handled by cucumber.js
 *
 * This file exists for:
 * - IDE support (VS Code Playwright extension)
 * - Browser installation (pnpm exec playwright install)
 * - Future non-BDD test additions
 *
 * Run BDD tests with: pnpm test
 * See cucumber.js for BDD configuration
 */

import { defineConfig } from '@playwright/test';

export default defineConfig({
  // Empty stub - BDD tests run via cucumber.js
});
