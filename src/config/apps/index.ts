/**
 * App Configuration Barrel Export
 *
 * Exports all app configurations for easy importing.
 *
 * @example
 * ```ts
 * import { sauceDemoConfig, petstoreConfig, restfulBookerConfig } from '@/config/apps';
 * ```
 */

// Re-export types for convenience
export type { AppConfig } from '@/types/config.types';
